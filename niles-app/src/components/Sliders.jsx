import { steps } from "../utils";
const Slider = () => {
  return (
    <section className="section-spacing pt-101 lg:pt-100 xl:pt-110 bg-pry1">
      <h2 className=" section-header">Supercharge Your Store With Nile!</h2>
      <div className="mx-auto gap-10 flex-container  ">
        {/* {steps.map((item) => (
        ))} */}
      </div>
    </section>
  );
};

export default Slider;